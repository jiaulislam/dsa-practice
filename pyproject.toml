[project]
name = "dsa-practice"
version = "0.1.0"
description = "learning data structures and algorithms"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "pre-commit>=4.3.0",
    "pytest>=8.4.2",
    "ruff>=0.14.2",
]
